services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: releaf-sql
    environment:
      SA_PASSWORD: "StrongP@ssw0rd123!"
      ACCEPT_EULA: "Y"
    ports:
      - "11433:1433"
    volumes:
      - sql_data:/var/opt/mssql

volumes:
  sql_data:
